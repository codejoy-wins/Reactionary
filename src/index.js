import React from 'react';
import ReactDom from 'react-dom';
import Master from './components/Master'; // need the ./ path but don't need .js

ReactDom.render(<Master/>,document.getElementById("root"));
